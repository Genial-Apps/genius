{
  "name": "genius-academy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "emulator:test": "(cd firebase/functions && firebase functions:config:get || true) && echo \"Start emulator in one terminal: firebase emulators:start --only functions\" && echo \"Then run: export VITE_FUNCTIONS_BASE_URL=http://127.0.0.1:5001/<PROJECT_ID>/us-central1 && npm run dev\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "expo start",
    "eas:update": "eas update --branch main --non-interactive",
    "eas:build:android": "eas build -p android --non-interactive"
  },
  "dependencies": {
    "lucide-react": "^0.562.0",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.34.0",
    "react": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
